/**
 * Data layer: MediaRepository, MediaDao (Room), EncryptedFileStore, KeyStoreProvider,
 * and SecurePrefs. Metadata only in Room; encrypted media bytes live under
 * filesDir/vault/. Populated in later tasks.
 */
package com.privatemediavault.data
