<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      OFFLINE GUARANTEE (Requirement 3):
      This application declares NO permissions that grant network access.
      Specifically, it does NOT request:
        - android.permission.INTERNET
        - android.permission.ACCESS_NETWORK_STATE
      All media, PIN data, and application data are stored and processed
      exclusively on the local device. Do not add network permissions here.
    -->

    <application
        android:name=".VaultApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrivateMediaVault">

        <activity
            android:name=".VaultActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PrivateMediaVault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
